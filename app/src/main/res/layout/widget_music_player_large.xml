<?xml version="1.0" encoding="utf-8"?>
<!--
    MuSicX Project (C) 2026
    Large 4x4 (and taller) "now playing" widget. Vertical layout:
    portrait album art + title/artist/app logo on top, prev/play/next
    controls below, progress bar at the bottom.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@android:id/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="16dp"
    android:gravity="center_horizontal"
    android:theme="@style/Theme.Widget.Metrolist">

    <!-- Top section: portrait album art + (title / artist / app logo) -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Portrait album art (left) -->
        <ImageView
            android:id="@+id/widget_album_art"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:scaleType="centerCrop"
            android:src="@mipmap/ic_launcher"
            android:contentDescription="@string/album_art" />

        <!-- Title / artist / logo column (right) -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:orientation="horizontal"
            android:layout_marginStart="12dp"
            android:gravity="top">

            <LinearLayout
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:orientation="vertical"
                android:layout_gravity="center_vertical">

                <TextView
                    android:id="@+id/widget_song_title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/not_playing"
                    android:textSize="16sp"
                    android:textStyle="bold"
                    android:textColor="@color/widget_text_primary"
                    android:maxLines="2"
                    android:ellipsize="end" />

                <TextView
                    android:id="@+id/widget_artist_name"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/tap_to_play"
                    android:textSize="13sp"
                    android:textColor="@color/widget_text_secondary"
                    android:maxLines="2"
                    android:ellipsize="end"
                    android:layout_marginTop="2dp" />
            </LinearLayout>

            <!-- Small app logo (top-right) -->
            <ImageView
                android:id="@+id/widget_app_logo"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="8dp"
                android:scaleType="fitCenter"
                android:src="@mipmap/ic_launcher"
                android:contentDescription="@string/app_name" />
        </LinearLayout>
    </LinearLayout>

    <!-- Controls row: previous / play-pause / next -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:layout_marginTop="10dp"
        android:baselineAligned="false">

        <ImageButton
            android:id="@+id/widget_prev_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_widget_skip_previous"
            android:contentDescription="@string/previous" />

        <FrameLayout
            android:id="@+id/widget_play_pause_container"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:layout_marginHorizontal="8dp"
            android:background="@drawable/widget_play_pill_bg">

            <ImageView
                android:id="@+id/widget_play_pause"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_widget_play"
                android:contentDescription="@string/play_pause" />
        </FrameLayout>

        <ImageButton
            android:id="@+id/widget_next_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:scaleType="center"
            android:background="@android:color/transparent"
            android:src="@drawable/ic_widget_skip_next"
            android:contentDescription="@string/next" />
    </LinearLayout>

    <!-- Progress bar -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="6dp"
        android:layout_marginTop="10dp">

        <ImageView
            android:id="@+id/widget_progress_track"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/widget_progress_track"
            android:scaleType="fitXY" />

        <ImageView
            android:id="@+id/widget_progress_fill"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:src="@drawable/widget_progress_clip"
            android:scaleType="fitXY" />
    </FrameLayout>

</LinearLayout>
